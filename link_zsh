cd $(dirname $0)
mkdir -p $(dirname $HOME/.zprezto) && ln -sni $PWD/confs/.zprezto $HOME/.zprezto
mkdir -p $(dirname $HOME/.zlogin) && ln -sni $PWD/confs/.zprezto/runcoms/zlogin $HOME/.zlogin
mkdir -p $(dirname $HOME/.zlogout) && ln -sni $PWD/confs/.zprezto/runcoms/zlogout $HOME/.zlogout
mkdir -p $(dirname $HOME/.zpreztorc) && ln -sni $PWD/confs/.zprezto/runcoms/zpreztorc $HOME/.zpreztorc
mkdir -p $(dirname $HOME/.zprofile) && ln -sni $PWD/confs/.zprezto/runcoms/zprofile $HOME/.zprofile
mkdir -p $(dirname $HOME/.zshenv) && ln -sni $PWD/confs/.zprezto/runcoms/zshenv $HOME/.zshenv
mkdir -p $(dirname $HOME/.zshrc) && ln -sni $PWD/confs/.zprezto/runcoms/zshrc $HOME/.zshrc
mkdir -p $(dirname $HOME/.rc) && ln -sni $PWD/confs/.rc $HOME/.rc
