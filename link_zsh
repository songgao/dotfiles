mkdir -p $(dirname $HOME/.zprezto) && ln -sni /Users/songgao/repo/dot/confs/.zprezto $HOME/.zprezto
mkdir -p $(dirname $HOME/.zlogin) && ln -sni /Users/songgao/repo/dot/confs/.zprezto/runcoms/zlogin $HOME/.zlogin
mkdir -p $(dirname $HOME/.zlogout) && ln -sni /Users/songgao/repo/dot/confs/.zprezto/runcoms/zlogout $HOME/.zlogout
mkdir -p $(dirname $HOME/.zpreztorc) && ln -sni /Users/songgao/repo/dot/confs/.zprezto/runcoms/zpreztorc $HOME/.zpreztorc
mkdir -p $(dirname $HOME/.zprofile) && ln -sni /Users/songgao/repo/dot/confs/.zprezto/runcoms/zprofile $HOME/.zprofile
mkdir -p $(dirname $HOME/.zshenv) && ln -sni /Users/songgao/repo/dot/confs/.zprezto/runcoms/zshenv $HOME/.zshenv
mkdir -p $(dirname $HOME/.zshrc) && ln -sni /Users/songgao/repo/dot/confs/.zprezto/runcoms/zshrc $HOME/.zshrc
mkdir -p $(dirname $HOME/.rc) && ln -sni /Users/songgao/repo/dot/confs/.rc $HOME/.rc
