cd $(dirname $0)
mkdir -p $(dirname $HOME/.gitconfig) && ln -sniT $PWD/confs/.gitconfig $HOME/.gitconfig
mkdir -p $(dirname $HOME/.gitignore_global) && ln -sniT $PWD/confs/.gitignore_global $HOME/.gitignore_global
