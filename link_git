cd $(dirname $0)
mkdir -p $(dirname $HOME/.gitconfig) && ln -sni $PWD/confs/.gitconfig $HOME/.gitconfig
mkdir -p $(dirname $HOME/.gitignore_global) && ln -sni $PWD/confs/.gitignore_global $HOME/.gitignore_global
