cd $(dirname $0)
mkdir -p $(dirname $HOME/.vimrc) && ln -sniT $PWD/confs/.vimrc $HOME/.vimrc
mkdir -p $(dirname $HOME/.ycm_extra_conf.py) && ln -sniT $PWD/confs/.ycm_extra_conf.py $HOME/.ycm_extra_conf.py
